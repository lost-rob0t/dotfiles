name: Refresh Bixby lock

on:
  push:
    branches:
      - agent/all-unstable

permissions:
  contents: write

concurrency:
  group: refresh-bixby-lock
  cancel-in-progress: true

jobs:
  lock:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/all-unstable
          fetch-depth: 0
      - uses: cachix/install-nix-action@v31
      - name: Update Bixby input lock
        run: nix flake update bixby-studio
      - name: Commit lockfile if changed
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- flake.lock; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- flake.lock
          git commit -m "chore(flake): update Bixby Studio input"
          git push origin HEAD:agent/all-unstable
