# -*- mode: snippet -*-
# name: async-shell
# key: async-shell
# --

#+begin_src sh :async :results  ${1:$$(yas-choose-value '("ouput" "table" "raw"))} ${2:$$(yas-choose-value '("replace" "append" "prepend"))}
$0
#+end_src