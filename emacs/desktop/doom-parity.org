#+TITLE: Doom -> Native Desktop Parity Contract
#+STARTUP: overview

This is the acceptance contract for replacing the desktop Doom runtime.
A package being present is not parity. A module is done when its user-visible
behavior is reproduced natively and covered by a smoke test or an explicit
manual check.

Doom remains the default profile until the pending entries below are closed.

* Completion
| Doom module | Native implementation | State |
|-------------+-----------------------+-------|
| company | =company= global mode + Doom-like popup defaults | TESTED |
| ivy | Ivy/Counsel/Swiper + ivy-rich + ivy-xref + counsel-projectile + amx | TESTED |

* UI
| Doom module | Native implementation | State |
|-------------+-----------------------+-------|
| doom | =doom-themes= / =doom-outrun-electric= + solaire | TESTED |
| emoji +unicode | =emojify= + native Unicode setup | TESTED |
| hl-todo | =global-hl-todo-mode= | TESTED |
| modeline | =doom-modeline= with project/workspace state | TESTED |
| ophints | Evil Goggles pulse feedback | TESTED |
| popup +defaults | =display-buffer-alist= side-window policy | PARTIAL |
| unicode | =unicode-fonts-setup= on graphical startup | TESTED |
| vc-gutter +pretty | =diff-hl= + flydiff + hunk leader actions | TESTED |
| vi-tilde-fringe | =vi-tilde-fringe= | TESTED |
| workspaces | =persp-mode= + =SPC TAB ...= | TESTED |

* Editor
| Doom module | Native implementation | State |
|-------------+-----------------------+-------|
| evil +everywhere | Evil + evil-collection and Doom extension set | TESTED |
| fold | vimish-fold + evil-vimish-fold | TESTED |
| format +onsave | global Apheleia | TESTED |
| lispy | Lispy + Lispyville hooks | TESTED |
| parinfer | parinfer-rust-mode Lisp hooks | TESTED |
| snippets | yasnippet + yasnippet-snippets + Doom snippets | TESTED |
| word-wrap | visual-line + adaptive-wrap native bootstrap | TESTED |

* Emacs
| Doom module | Native implementation | State |
|-------------+-----------------------+-------|
| dired | Dired + Dirvish override/config | TESTED |
| electric | electric-pair-mode + Smartparens | TESTED |
| ibuffer | native ibuffer, Doom leader entry | TESTED |
| undo | undo-fu + undo-fu-session + Vundo entry | TESTED |
| vc | built-in VC + Magit + diff-hl hunk actions | TESTED |

* Term
| Doom module | Native implementation | State |
|-------------+-----------------------+-------|
| eshell | native Eshell + migrated aliases + popup/here entries | TESTED |
| vterm | vterm + Doom-style popup/here entries | TESTED |

* Checkers
| Doom module | Native implementation | State |
|-------------+-----------------------+-------|
| syntax | global Flycheck + flycheck-package | TESTED |

* Tools
| Doom module | Native implementation | State |
|-------------+-----------------------+-------|
| debugger | Dape command tree at =SPC d ...= | TESTED |
| direnv | envrc global mode | TESTED |
| docker | Dockerfile docset + dockerfmt integration when available | TESTED-CONFIG |
| editorconfig | global editorconfig-mode + leader entry | TESTED |
| eval +overlay | EROS on Emacs Lisp buffers | TESTED |
| lookup | dumb-jump xref fallback + Ivy xref + LSP | TESTED-CONFIG |
| lsp | lsp-deferred language hooks + Doom-style code leader actions | TESTED |
| magit +forge | Magit/Forge leader tree + private overrides | TESTED |
| make | native =+make/run= and =+make/run-last= wrappers | TESTED |
| pdf | native pdf-tools setup, fit-page/scaling, cleanup and popup policy | TESTED |
| rgb | rainbow-mode hooks for CSS/Sass/Stylus | TESTED |
| upload | Doom ssh-deploy defaults, safe locals, hooks and leader tree | TESTED |

* OS
| Doom module | Native implementation | State |
|-------------+-----------------------+-------|
| tty | KKP enabled for interactive terminal Emacs | TESTED |

* Languages
The native closure carries the packages selected by the old Doom modules rather
than only the packages named in =~/.doom.d/packages.org=. The current startup
also restores the high-use runtime paths for CC, Common Lisp/Sly, Emacs Lisp,
JavaScript/TypeScript, Nim, Nix, Org/Org-roam, Python, shell, web, JSON,
Markdown, PHP, and data files.

Language parity is still *PENDING-MANUAL* until each major mode is opened on
=unseen@flake= and its Evil, completion, syntax, formatting, lookup and LSP
behavior is exercised. That manual pass is required before the default profile
may switch.

* Apps and private configuration
| Feature | Native implementation | State |
|---------+-----------------------+-------|
| RSS +org | Elfeed + elfeed-org + elfeed-tube | TESTED-CONFIG |
| everywhere | Emacs Everywhere snippets + Hyprland/Niri compatibility; stock X11 path retained | TESTED-CONFIG |
| Org Agenda startup | =initial-buffer-choice= opens Agenda, not Doom dashboard | TESTED |
| Org capture/roam | migrated literate configuration + restored leader hierarchy | TESTED-CONFIG |
| GPTEL/MCP | migrated private config + native autoload layer | TESTED-CONFIG |
| StarIntel integration | local =starintel= library/config retained | TESTED-CONFIG |
| Temple/local Lisp | local load path retained | TESTED-CONFIG |
| Dslide | private functions and leader bindings retained | TESTED-CONFIG |

* Remaining blockers
** Reproducible Nix sources
The native closure must contain no placeholder fixed-output hashes. The custom
recipes for app-launcher, asoc, libvirt, opencl-mode, org-recoll,
org-timed-alerts, podman.el and sly-stepper still need their real source hashes
(or must be removed in favor of an already-packaged Nixpkgs Emacs package).

** Popup equivalence
The high-use popup families are covered, but Doom's popup module has a broader
rule engine than the native side-window compatibility layer. Keep this PARTIAL
until the physical comparison on =unseen@flake= exercises help, compilation,
Magit/Forge, Sly, vterm/eshell, warnings and PDF auxiliary buffers.

** Physical language/UI pass
CI can prove bindings, packages and hooks. It cannot prove that the GUI actually
looks right or that every language server works against the binaries installed
on =unseen@flake=. That pass remains mandatory.

* Merge gate
PR #232 is mergeable only when all of these are true:

1. Every Nix fixed-output source hash is real; no =lib.fakeHash= remains in the
   native Emacs closure.
2. Literate tangling produces a clean tree.
3. =tools/doom-parity-smoke.el= and =tools/pdf-parity-smoke.el= pass against the
   exact Home Manager Emacs closure without loading the Doom runtime.
4. Every unresolved parity entry above is implemented or explicitly shown to be
   intentionally unnecessary for the user's effective old Doom configuration.
5. The native profile is launched on =unseen@flake= and visually/behaviorally
   compared against Doom: theme, modeline, Agenda startup, Evil motions,
   leader menus, completion, project navigation, workspaces, popups, Magit,
   Org, vterm/eshell, Lisp editing, PDF viewing and LSP.
6. Only after that manual comparison does plain =emacs= switch away from Doom.
