# logos

Current NixOS 26.05 flake configuration for the `logos` workstation.

```sh
nix build
sudo nixos-rebuild build --flake .#logos
sudo nixos-rebuild switch --flake .#logos
```

The active host keeps Docker and excludes libvirt, KVM host modules, virt-manager,
SPICE, Waydroid, Wine, Steam, Sunshine, Prism Launcher, and other game/VM tooling.

This refresh was generated by GPT at the repository owner's request.
